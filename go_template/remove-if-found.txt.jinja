{#-
Files the template used to render and no longer does. `copier update` leaves a
stale file in place, so _copier_post_generation.py deletes everything listed here
and then deletes this file. One path per line, relative to the project root.
-#}
.github/workflows/publish_python.yml
Formula/{{ project_name }}.rb
