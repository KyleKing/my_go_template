# {{ project_name }}

{{ project_description }}

## Installation

{% if project_type == 'cli' %}
{% if use_goreleaser %}
### GH CLI Extension

> **Note:** Requires a [GitHub Release]({{ repository_provider }}/{{ repository_namespace }}/{{ project_name }}/releases) with precompiled binaries. See [CONTRIBUTING.md](CONTRIBUTING.md#releases) for creating the first release.

```bash
gh extension install {{ repository_namespace }}/{{ project_name }}
```

### Homebrew

goreleaser publishes a cask to [`{{ repository_namespace }}/homebrew-tap`]({{ repository_provider }}/{{ repository_namespace }}/homebrew-tap) with each release, so the install needs `--cask`:

```bash
brew install --cask {{ repository_namespace }}/tap/{{ project_name }}
```

### Go Install
{% else %}
### Go
{% endif %}

```bash
go install {{ module_path }}/cmd/{{ project_name }}@latest
```
{% elif project_type == 'library' %}

```bash
go get {{ module_path }}
```

### Usage

```go
import "{{ module_path }}"
```
{% endif %}

## Development

```bash
mise install && hk install --mise
mise run ci
```

See [CONTRIBUTING.md](CONTRIBUTING.md) for full development workflow.
